import 'package:flutter/material.dart';

class RepoList extends StatelessWidget {
  const RepoList({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Repo List");
  }
}
