import 'package:flutter/material.dart';

class GestureQuickSettings extends StatelessWidget {
  const GestureQuickSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Gesture Quick Settings");
  }
}
