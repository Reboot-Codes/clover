import 'package:flutter/material.dart';

class RepoGestureList extends StatelessWidget {
  const RepoGestureList({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Gesture List");
  }
}
