import 'package:flutter/material.dart';

class RepoOverview extends StatelessWidget {
  const RepoOverview({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Overview");
  }
}
