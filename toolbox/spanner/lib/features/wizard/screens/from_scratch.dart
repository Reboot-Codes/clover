import 'package:flutter/material.dart';

class WizardFromScratch extends StatelessWidget {
  const WizardFromScratch({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("From Scratch.");
  }
}
